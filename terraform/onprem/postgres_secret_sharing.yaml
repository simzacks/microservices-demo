apiVersion: v1
kind: Secret
metadata:
  name: postgres-ha-app
  namespace: default # <-- Created right where your microservices live
  annotations:
    # Pull the exact secret data live out of the infra workspace
    reflector.v1.k8s.emberstack.com/reflects: "infra/postgres-ha-app"
data: {} # Leave empty! The operator automatically mirrors the data keys here.
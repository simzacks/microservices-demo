apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/simzacks/microservices-demo.git'
    targetRevision: main
    path: gitops/workloads/frontend
  destination:
    server: 'https://kuberness.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
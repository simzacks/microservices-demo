apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: recommendationservice
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/simzacks/microservices-demo.git'
    targetRevision: main
    path: gitops/workloads/recommendationservice
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
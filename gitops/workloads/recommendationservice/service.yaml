apiVersion: v1
kind: Service
metadata:
  name: recommendationservice
  namespace: default
  labels:
    app: recommendationservice
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 8080
      targetPort: 8080
  selector:
    app: recommendationservice
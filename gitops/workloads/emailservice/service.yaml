apiVersion: v1
kind: Service
metadata:
  name: emailservice
  namespace: default
  labels:
    app: emailservice
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 8080
      targetPort: 8080
  selector:
    app: emailservice